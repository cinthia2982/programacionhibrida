import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.evaluacion1proghibrida',
  appName: 'evaluacion1proghibrida',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
